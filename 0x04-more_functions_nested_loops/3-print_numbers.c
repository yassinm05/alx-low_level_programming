#include "main.h"
/**
 * print_numbers - check the code.
 *
 * Return: Always 0.
 */

void print_numbers(void)
{
	char letter;

	for (letter = 48; letter <= 57; letter++)
		putchar(letter);
	putchar('\n');
}
